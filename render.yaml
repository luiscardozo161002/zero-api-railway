services:
  - type: web
    name: zero-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - fromGroup: zero-api-secrets # Opcional: si usas grupos de secretos
